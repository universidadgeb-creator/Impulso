/**
 * Configuración compartida de la plataforma — Ruta GEB / Impulso GEB.
 * BACKEND es la URL del Apps Script Web App que recibe identidad y entregas.
 * [verificar] sustituir por la URL real cuando esté publicada — mismo
 * placeholder que ya usa el formulario de entrega en momento-guia.html.
 */
const CONFIG = {
  BACKEND: 'YOUR_APPS_SCRIPT_WEB_APP_URL'
};
